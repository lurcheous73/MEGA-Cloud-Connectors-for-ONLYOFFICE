# MEGA Cloud connector

Implementation area for the normal `mega.nz` connected-storage provider.

The first task is interface discovery: map ONLYOFFICE's current cloud-provider contract against the official MEGA Cloud API before committing to authentication/session or filesystem adapter details.
